#include <stdio.h>

/**
 * print_alphabet - prints lowercase alphabet
 * Return: 0
 */
void print_alphabet(void)
{
    char alpha;
    for (alpha = 'a'; alpha <= 'a'; alpha++)
    {
        putchar(alpha);
    }
    putchar('\n');
}
